{
  "name": "001destake",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ 001destake",
    "create-local": "graph create --node http://localhost:8020/ 001destake",
    "remove-local": "graph remove --node http://localhost:8020/ 001destake",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 001destake"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.1",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
